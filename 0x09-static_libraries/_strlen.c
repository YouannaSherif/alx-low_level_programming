#include "main.h"
#include <string.h>

/**
 * _strlen -calculate the length of string
 *
 * @s: is a paramter
 *
 * Return: 0 on succes
 */

int _strlen(char *s)
{
	int x;

	x = strlen(s);
	return (x);
}
