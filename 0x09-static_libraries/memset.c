#include "main.h"

/**
 * _memset -function
 * @s: string
 * @b: a parameter
 * @n: a parameter
 * Retturn: value
 */

char *_memset(char *s, char b, unsigned int n)
{
	*s = 'n';
	b = 'n';
	n = 0;
	return (s);
}
