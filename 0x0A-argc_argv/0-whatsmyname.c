#include <stdio.h>
/**
 * main -Entery point
 * @argc: counter
 * @argv: array of pointers
 * Return: 0 on success
 */
int main(int argc, char **argv)
{
		printf("%s\n", argv[argc - 1]);
	return (0);
}
