#include "main.h"
#include <stdio.h>

/**
 * print_last_digit -prints last digit of a number
 *
 * @x: is a parameter
 *
 * Return: 0 on success
 */

int print_last_digit(int x)
{
	x %= 10;
	return (x);
}
