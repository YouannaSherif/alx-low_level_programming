#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _puts -prints string to stdout followed by a new line
 *
 * @str: a pointer
 *
 * Return: 0 on success
 */

void _puts(char *str)
{
	puts(str);
}
